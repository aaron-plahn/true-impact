export { Module } from '@nestjs/common';

/**
 * TODO We should wrap (in true decorator fashion!) NestJS module inside our own definition. This will
 * allow us to reuse logic for registering command handlers, event consumers, and domain model \ view model
 * schemas.
 */
