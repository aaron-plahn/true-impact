name: "Security Scan (CodeQL)"
on:
  push:
    pull_request: 
      branches
        - integration
        - staging
    schedule:
      - cron '0 0 * * 0' # Every Sunday at midnight

  jobs:
    codeql:
      name: GitHub CodeQL Scan
      runs-on:
        ubuntu-latest
      permissions:
        security-events: write
        actions: read
        contents: read
      steps:
        - name: Checkout repository
          uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

        - name: Initialize codeql
          uses: github/codeql-action/init@v3 # TODO pin this
          with:
            languages: 'javascript-typescript'
            queries: security-extended,security-and-quality

        - name: Autobuild
          uses: github/codeql-action/autobuild@v3 #TODO pin

        - name: CodeQL Analysis
          uses: github/codeql-action/analyze@v3 # TODO pin
          with: 
            category: "/language:javascript-typescript"
    # TODO opt-in
    # npm-audit:
    #   name: NPM Dependency audit
    #   runs-on: ubuntu-latest
    #   steps:
    #     - name: Checkout repository
    #       uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

    #     - name: Setup Node.js
    #       uses: actions/setup-node/@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
    #       with:
    #         node-version: 24.13.0
    #         cache: 'npm'
    #     - name: NPM Audit
    #       run: yarn audit --groups dependencies

    # secret-scan:
    #   name: Secret & Dotenv Leak Scan
    #   runs-on: ubuntu-latest
    #   steps:
    #   - name: Checkout repository
    #     uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
    #     with:
    #       fetch-depth: 0 # Deep history check

    #   - name: TruffleHog OSS Secret Scan
    #     uses: trufflesecurity/trufflehog-oss-action@main # TODO pin this to an SHA
    #     with:
    #       base: ${{ github.event.repository.default_branch }}
    #       head: HEAD
    #       extra_args: --only-verified

    # TODO dependabot